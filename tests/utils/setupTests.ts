import '@testing-library/jest-dom'

const API_URL = "http://localhost:mock"

// Mock your config file

// jest.mock("@/config/endpoints.config",()=>({
//     endpoints : {
//         login: `${API_URL}/auth/login`,
//     }
// })) 